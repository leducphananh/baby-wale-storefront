# Storefront Phase S2.3 — High-Fidelity Design

> **Hi-fi canvas:** https://claude.ai/code/artifact/378cf72c-a832-4c2d-8e36-c6ac30c74640
> — a viewable Claude Design canvas of the north-star + key screens + a component sheet +
> a token board (see §12). Figma was not usable this phase (§0).

**Status:** High-fidelity design of the Baby Wale storefront in **Direction A — Soft
Trust Commerce**, plus an **exploratory token proposal** for S2.4. This is a
**PROPOSAL, not the approved design system.** S2.4 audits, refines, and freezes tokens.

**Date:** 2026-09-10. **Depends on:** S0, S0.5, S1, S2.1, S2.2.

---

## 0. Figma access — what actually happened

The task specifies Figma as the primary output. Figma access was inspected first
(`whoami`):

| Fact | Value |
| --- | --- |
| Authenticated Figma account | `lobantaphoa@gmail.com` — "Lỗ Bán Tạp Hoá's team" |
| Plan tier | **Starter** |
| Seat | **View** |
| MCP rate limit (this seat/tier) | **Up to 20 tool calls per month** |
| `use_figma` (design authoring) | **rate-limited** (not on the exempt list) |

**A "View" seat cannot edit Figma files, and 20 calls/month cannot build a design
system + ~25 screens + ~30 components.** Attempting it would exhaust the user's monthly
quota and leave a broken/empty file. So **no Figma file was created.**

**What was produced instead:**

1. **This document** — a build-ready high-fidelity design specification: exploratory
   tokens (with real candidate values), full component anatomy + states, per-screen
   layouts (mobile 390 / desktop 1440) with concrete measurements, and text wireframes.
   Precise enough for (a) S2.4 to ratify tokens, (b) a designer with a Full/Dev Figma
   seat to build the file directly, (c) implementation (S4+) to build screens from it.
2. **A Claude Design canvas** — a real, viewable multi-artboard hi-fi visual of the
   highest-value screens (mobile Product Detail = north-star, Home, Catalog, Product
   Detail OOS, Cart with a price-changed line, Checkout, Order Success COD; desktop
   Product Detail + Checkout; a Foundations token board; a Components state sheet).
   Link + notes in §12 and the completion report.

**Handoff item:** building the full Figma file (all 25 screens, all component variants)
requires a **Figma Full or Dev seat** (Professional plan or higher). Everything needed
to build it is in this document.

---

## 1. Locked inputs

- **Direction A — Soft Trust Commerce** is approved (VD1). No B/C alternates.
- **VD2:** no deviation toward B or C.
- **VD3:** category tiles are **icon/graphic-led** (no dependency on category
  photography); product photos are assumed **varied supplier sources** → normalized by
  one square `contain` frame with padding, never cropping labels.
- **VD4:** **no unsupported claims** ("100% chính hãng" etc.). Trust content = factual
  only (COD / bank transfer, staff confirms every order, real contact placeholder,
  shipping fee confirmed by staff).
- **D1:** completed-order label = **"Đơn hàng đã được xác nhận"** (never implies
  delivery). **D2:** no Add-to-Cart on `ProductCard`. **D3:** static Home hero, no
  carousel. **D4:** no return/exchange policy rendered; design reserves an optional
  slot only.
- **Research correction (task §3):** `ProductCard` in any listing shows **ONE** primary
  product image — **no thumbnails in list context**. Multiple thumbnails belong to
  **Product Detail** only (when >1 image exists). *(S2.2 §5.2 carried a general-UX
  phrase "3+ thumbnails … in list context" — corrected and not applied.)*
- **Sticky model (two-tier):** one compact sticky header + at most one bottom
  transactional bar. Product mobile = header + Add-to-Cart bar. Cart mobile = header +
  Checkout bar. **Checkout mobile = header only; submit in-flow.** Never two bottom
  bars.
- **Font:** Be Vietnam Pro (S1 foundation) — kept. No new font.
- Inputs read in full: `CLAUDE.md`, `docs/S0-requirements-and-architecture.md`,
  `docs/design/S2.1-…md`, `docs/design/S2.2-…md`, `docs/README.md`; skills
  `storefront-ui-design`, `ecommerce-ux`, `mobile-first-storefront`, `design-system`,
  `figma-to-code`, `visual-consistency`, `accessibility`, `next-image-storefront`,
  `vietnamese-ecommerce-ui`, `storefront-error-handling`, `baby-wale-domain`.

---

## 2. Exploratory token proposal

> **PROPOSED — REQUIRES S2.4 APPROVAL.** Concrete values so the hi-fi screens are real.
> Not production tokens. Do not update Tailwind/theme. S2.4 verifies contrast, samples
> exact brand blue/pink from the logo asset, and ratifies the scale.

### 2.1 Color (semantic roles → candidate hex)

| Token (role) | Candidate | Notes / contrast (approx, vs stated bg) |
| --- | --- | --- |
| `color.bg` (page) | `#FBFAF8` warm paper | primary text 12.8:1 ✓ AAA |
| `color.surface` (card, sheet) | `#FFFFFF` | primary text 13.6:1 ✓ AAA |
| `color.surface.subtle` (fills, summary panel, image frame) | `#F4F2EE` warm | + explore cool alt `#F1F4F8` |
| `color.text` (primary) | `#1F2A37` deep slate | on `#FBFAF8` ≈ 12.8:1 ✓ |
| `color.text.muted` | `#5B6673` | on `#FFFFFF` ≈ 5.6:1 ✓ AA · on `#FBFAF8` ≈ 5.3:1 ✓ AA |
| `color.border` (hairline) | `#E7E3DC` (on paper) / `#EAE7E1` (on white) | non-text — 1.3:1, decorative |
| `color.primary` (Trust Blue — CTA, links, active) | `#2563B0` | white on it ≈ 5.9:1 ✓ AA |
| `color.primary.hover` / `.pressed` | `#1E4E8C` / `#173F71` | white ≈ 7.6:1 / 10:1 ✓ |
| `color.primary.tint` (active row bg, link hover bg) | `#EAF2FB` | text `#1F2A37` ≈ 11:1 ✓ |
| `color.accent` (Warm Pink — cart badge, "Mới" chip, selected tint) | `#D64C7C` | white on it ≈ 4.0:1 — **OK for a ≤2-digit numeral / non-text glyph only; S2.4 confirm; fallback = Trust Blue badge** |
| `color.accent.tint` | `#FCEEF3` | text `#7A1F3D` ≈ 8:1 ✓ |
| `color.success` (in-stock, confirmed) | text `#177A4E` on tint `#E7F4EE` | ≈ 5.0:1 ✓ — **badge uses tint bg + dark text, not white-on-green** |
| `color.warning` | text `#8A560A` on tint `#FBF1E1` | ≈ 6.5:1 ✓ |
| `color.danger` (out-of-stock, error, blocking) | text `#B02E2E` on tint `#FBECEC` | ≈ 6.0:1 ✓ · solid `#C23B3B` + white ≈ 4.6:1 (icon/short) |
| `color.focus` (focus ring) | `#1D6FE0`, 2px ring + 2px `#FFFFFF` offset | visible on white, tint, and colored buttons |

**Restraint rules baked in:** page bg is the only large surface tint (warm paper);
blue and pink are never large fills; one primary CTA color everywhere; category tiles
share one surface tone (no rainbow).

### 2.2 Typography (Be Vietnam Pro — px @ 1×, mobile / desktop)

| Role | Size / line-height / weight | Use |
| --- | --- | --- |
| `type.display` | 28/36/700 · 40/48/700 | Home hero H1 |
| `type.h1` | 22/30/600 · 28/36/600 | page H1 (Catalog, Cart, Checkout, Success) |
| `type.h2` | 18/26/600 | section headings |
| `type.h3` | 16/24/600 | sub-sections, card group headers |
| `type.body` | 15/24/400 | body copy, descriptions |
| `type.bodySm` | 13/20/400 | helper text, secondary |
| `type.caption` | 12/16/400 · 500 | metadata, breadcrumb, timestamps |
| `type.productName.card` | 14/20/500 · **2-line clamp, reserve 40px** | `ProductCard` name |
| `type.productName.pdp` | 20/28/600 · 24/32/600 | Product Detail H1 |
| `type.price.lg` | 24/30/700 · 28/34/700 · **tabular nums** | PDP price |
| `type.price.card` | 15/20/700 · **tabular nums** | `ProductCard` price, cart line total |
| `type.price.total` | 18/26/700 · **tabular** | "Tổng tiền hàng" |
| `type.button` | 15/20/600 · sentence case | all buttons |
| `type.label` | 13/18/500 | field labels, badge text |
| `type.input` | 15/22/400 | field input text |

Diacritic clearance: card/label line-heights kept ≥ 1.4× to clear stacked marks
(ế, ữ, ậ, ằ). `letter-spacing: 0`. Numerals: enable `font-variant-numeric: tabular-nums`
on all price roles.

### 2.3 Spacing (4px base)

`2, 4, 8, 12, 16, 20, 24, 32, 40, 48, 64`

| Context | Value |
| --- | --- |
| Mobile outer gutter | 16 |
| Mobile section gap (between page sections) | 32 |
| Mobile product grid | 2 col · column-gap 12 · row-gap 20 |
| Card inner padding | 12 |
| Field vertical rhythm (label→field→helper) | 6 / — / 6 |
| Form field-to-field gap | 16 |
| Desktop page gutter | 24–32 · content max-width **1200** |
| Desktop section gap | 48–64 |

### 2.4 Radius

| Token | px | Use |
| --- | --- | --- |
| `radius.xs` | 6 | chips, badges, small tags |
| `radius.sm` | 10 | buttons, inputs, `ProductCard`, cart lines |
| `radius.md` | 14 | image frame, summary panels, category tiles, dialogs |
| `radius.lg` | 20 | bottom-sheet top corners, hero surface |

No pills, no blob radii. One scale, applied consistently.

### 2.5 Elevation

| Token | Value | Use |
| --- | --- | --- |
| `elevation.flat` | none — separated by space / hairline / tone | most content, static `ProductCard`, sections |
| `elevation.raise` | `0 1px 2px rgba(31,42,55,.06), 0 4px 12px rgba(31,42,55,.06)` | hovered/focused card, popover |
| `elevation.overlay` | top/edge hairline `1px #E7E3DC` **+** `0 8px 24px rgba(31,42,55,.10)` (sticky bottom bar casts the shadow upward) | cart drawer, bottom sheet, dialog, stuck header, sticky bottom bar |

Never a shadow on a resting `ProductCard`. One shadow language site-wide.

### 2.6 Container, grid, breakpoints, control sizing

| Item | Value (EXPLORATORY) |
| --- | --- |
| Breakpoints | `sm 480 · md 768 · lg 1024 · xl 1280` |
| Mobile content | full-bleed, 16px gutters (≈358 usable at 390) |
| Desktop content max-width | 1200, centered, 24–32 gutters |
| Product grid | 2 col ≤767 · 3 col 768–1023 · 4 col ≥1024 (gap 12/24) |
| PDP desktop split | gallery ~56% · purchase panel ~40% · gap 48 · panel sticky (top offset = header + 24) |
| Checkout desktop split | form ~62% · summary ~34% · gap 48 · summary sticky |
| Field height | 44 |
| Button height | 48 primary · 40 secondary/compact · 36 inline |
| Icon sizes | 20 (inline), 24 (nav/actions), 16 (badge) |
| Touch target min | 44 × 44 |
| Mobile header | 56 top row + 48 search row = **104** total; sticky |
| Desktop header | 64; sticky |
| Mobile sticky bottom bar | 64 + iOS safe-area inset |
| Mobile PDP gallery | full-width square (≈358 × 358 at 390) |

---

## 3. Component inventory (anatomy + states)

Each is **one Figma component** with variants. Reuse-first (`visual-consistency`).
Every interactive component has: `default · hover · focus-visible · pressed · disabled`
(+ `pending` where async). Status is **never color-only** — always text and/or
icon + shape.

### 3.1 `Button`

- **Variants:** `primary` (Trust Blue fill, white text), `secondary` (white fill,
  `#1F2A37` text, `#E7E3DC` border), `ghost` (no fill/border, Trust Blue text),
  `danger-quiet` (used only for destructive confirm — `#B02E2E` text, tint bg on hover).
- **Sizes:** `lg` 48h / 16–20 px-x / `radius.sm` · `md` 40h · `sm` 36h.
- **States:** default; hover (`primary` → `#1E4E8C`; `secondary` → `#FBFAF8` fill);
  focus-visible (`color.focus` 2px ring + 2px offset); pressed (`#173F71` / inset feel);
  disabled (`#EDEBE7` fill, `#9AA1AC` text, no shadow, `cursor:not-allowed`,
  `aria-disabled`); pending (label swaps to "Đang xử lý…" + 16px spinner, disabled).
- **Full-width** variant boolean for mobile primary CTAs.
- Label: sentence case, `type.button`. Never all-caps.

### 3.2 `SearchField`

- 44h, `radius.sm`, `#FFFFFF` fill, `#E7E3DC` border, 20px search icon (leading),
  clear "✕" button (trailing, appears when non-empty, 44×44 hit area).
- Placeholder `#8A909A`: "Tìm sữa, bỉm, bình sữa…". **Visible label** (visually-hidden
  on mobile is acceptable; the icon + placeholder + `aria-label="Tìm sản phẩm"` carry
  it) — desktop shows a label or the field is clearly a search by icon + submit.
- States: default; focus (`color.focus` ring, border → `color.primary`); filled (clear
  button visible); submitting (spinner replaces search icon).

### 3.3 `TextField` / `TextArea`

- **Label above** (`type.label`, `#1F2A37`), 6px gap, field 44h (textarea min 88h),
  `radius.sm`, `#FFFFFF`, `#E7E3DC` border, 12px inner-x.
- Optional helper line below (`type.bodySm`, `#5B6673`).
- States: default; focus (`color.focus` ring + border `color.primary`); filled (value
  in `type.input` `#1F2A37`); **error** (border + ring `#C23B3B`, 16px alert icon
  leading or trailing, message below in `#B02E2E` `type.bodySm`, `aria-invalid` +
  `aria-describedby`); disabled (`#F4F2EE` fill, `#9AA1AC` text).
- Phone field: `inputmode="numeric"`, `type="tel"`, displayed grouped `09xx xxx xxx`.
- **Placeholder is never the label.**

### 3.4 `PaymentRow` (radio option, part of `PaymentMethodSelector`)

- Full-width selectable row: `radio` (left, 20px) · label (`type.h3`) · helper line
  (`type.bodySm`, `#5B6673`) · optional 20px method icon (right).
- Row: `#FFFFFF`, `#E7E3DC` border, `radius.sm`, 12–16 padding, ≥56h.
- **Selected:** border → `color.primary` (2px), radio filled, subtle `color.primary.tint`
  fill, a 16px check — **three signals, not color alone**.
- Focus-visible ring on the row. Disabled during submit (`#F4F2EE`).
- Two instances: "Thanh toán khi nhận hàng (COD)" / "Chuyển khoản ngân hàng".

### 3.5 `Price`

- One VND format everywhere: `315.000 ₫` (thousands `.`, non-breaking space before
  `₫`). `type.price.*` per context, `tabular-nums`, `#1F2A37`.
- Boolean `strikethrough` (muted `#8A909A`, for the old price in a price-changed line).
- No decimals ever. Integer VND.

### 3.6 `StockBadge`

- Chip: 16px icon + `type.label` text, `radius.xs`, 4/8 padding.
- **In stock:** `#E7F4EE` bg, `#177A4E` text, check-circle icon, "Còn hàng". *(May be
  omitted on `ProductCard` when in stock if S2.4 prefers; always shown on PDP.)*
- **Out of stock:** `#FBECEC` bg, `#B02E2E` text, slash-circle icon, "Hết hàng".
- Never a number. Never "Sắp hết hàng".

### 3.7 `QuantitySelector`

- `[ − ]  [ 2 ]  [ + ]` — each button 44×44, `radius.sm`, `#FFFFFF`, `#E7E3DC` border;
  value 44w center, `type.h3`, `tabular`.
- `aria-label` "Giảm số lượng" / "Tăng số lượng"; value is a labelled `input` /
  `spinbutton`.
- States: default; `−` disabled at min 1; `+` disabled at UX max (with a tooltip/inline
  "Tối đa N"); whole control disabled when out of stock; focus ring per button.

### 3.8 `AddToCartButton`

- `Button` `primary` `lg` `full-width`, label "Thêm vào giỏ", leading 20px cart-plus
  icon.
- **Out of stock:** disabled, label "Hết hàng", no icon (or a slash icon). Still looks
  like a button, clearly inactive.
- **Pending:** "Đang thêm…" + spinner.
- On success: fires a `Toast` ("Đã thêm vào giỏ" + "Xem giỏ hàng" link) and the header
  cart count updates; an `aria-live="polite"` region announces it.

### 3.9 `ProductCard` — the reusability anchor

**One component. No Add-to-Cart. Whole card is a link to `/san-pham/[slug]`.**

Anatomy (top→bottom), card = `#FFFFFF`, `radius.sm`, `#EAE7E1` hairline border, `elevation.flat`, 12px inner padding:

1. **Image frame** — square 1:1, `#F4F2EE` bg, `radius.md`, image `object-fit: contain`
   with ~10% internal padding. One image only. Never crops label text.
2. **Brand** — `type.caption` `#5B6673`, 1 line, uppercase-tracking optional.
3. **Name** — `type.productName.card`, `#1F2A37`, **2-line clamp, reserved 40px height**
   so price/stock never shift between cards.
4. **Unit** — `type.caption` `#5B6673`, only when meaningful ("Lon 800g").
5. **Price** — `type.price.card`, `#1F2A37` — the loudest text after the image.
6. **StockBadge** — only rendered as "Hết hàng" (in-stock badge omitted on the card to
   reduce noise; S2.4 may re-add).

**Variants / states:** `default` · `hover` (desktop: `elevation.raise` + border →
`#D8D3CA`, image scales 1.02 with `prefers-reduced-motion` guard) · `focus-visible`
(`color.focus` 2px ring around the whole card, 2px offset) · `out-of-stock` (full
colour + legible; "Hết hàng" badge overlaid on the image bottom-left; image at 92%
opacity max) · `missing-image` (see 3.18) · `long-name` (2-line clamp + ellipsis, no
layout shift).

Card min-width at 390 (2-up, 16 gutter, 12 gap) ≈ **171px**; image ≈ 147px square.

### 3.10 `Header` (desktop) / `MobileHeader`

**Desktop (64h, `#FFFFFF`, bottom hairline `#EAE7E1`, sticky; stuck → `elevation.overlay` hairline+shadow):**
`[logo]` (left, ~120w) · `[Sản phẩm] [Danh mục ▾]` (nav, `type.body` `#1F2A37`) ·
`[SearchField]` (center, flex-grow, max ~480) · `[Tra cứu đơn hàng]` (quiet ghost link,
`type.bodySm`) · `[cart icon + badge]` (right, 44×44).
No login/account.

**Mobile (104h total, sticky):**
- Row 1 (56h): `[≡ hamburger 44×44]` · `[logo centered ~110w]` · `[cart 44×44 + badge]`.
- Row 2 (48h): full-width `SearchField` (16 gutters).
Cart badge: `color.accent` `#D64C7C` circle, white count, top-right of the cart icon,
16px, `aria-label="Giỏ hàng, {n} sản phẩm"`.

### 3.11 `MobileNavDrawer`

Left-slide sheet, ~86% width, `#FFFFFF`, `elevation.overlay`, `radius` 0 (edge-to-edge).
Header: "Danh mục" + close 44×44. List (`type.body`, 48h rows, hairline dividers):
`Trang chủ` · `Sản phẩm` · **`Danh mục`** (expandable disclosure → indented category
list) · `Tra cứu đơn hàng` · `Liên hệ`.
Focus trapped; `Esc` / scrim tap / close button dismiss; focus returns to the hamburger.

### 3.12 `Breadcrumbs`

- Desktop: `Trang chủ / Bỉm & Tã / Bỉm quần Merries…` — `type.caption` `#5B6673`,
  separators `/`, last item `#1F2A37` (not a link), `aria-current="page"`.
- Mobile: compact — `‹ Bỉm & Tã` (single back affordance to the parent category), 44h
  tap area.

### 3.13 `CategoryTile`

Icon-led (VD3). Tile: `#FFFFFF`, `#EAE7E1` hairline, `radius.md`, aspect ~4:3 or square,
centered 32px line-icon in `color.primary` on a `#EAF2FB` 48px rounded chip, label
below (`type.h3`, `#1F2A37`). Hover: `elevation.raise`. **One surface tone for all
tiles — no per-category colour.** Slot supports swapping the icon-chip for a photo
thumbnail later with no layout change.
Sample set: Sữa cho bé · Bỉm & Tã · Bình sữa & Ti · Ăn dặm · Chăm sóc bé · Đồ dùng mẹ.

### 3.14 `CartLineItem`

Row (`#FFFFFF`, hairline divider between, 12 padding): `[image 64×64 frame]` ·
`[brand caption / name 2-line / unit caption]` (flex-grow) · right column: `[remove ✕
32×32, aria-label "Xoá {name} khỏi giỏ"]`, `[QuantitySelector compact 36h]`,
`[line total — type.price.card]`.

**States:**
- `default`.
- `price-changed` — row gets a `color.primary.tint` left border (3px) + a line under
  the name: "Giá đã thay đổi" (`type.bodySm`, `#5B6673`) + old price (`Price`
  strikethrough) → new price (`Price` normal). **Not** red. Informational.
- `qty-adjust` — `color.warning` left border + "Chỉ còn 3 sản phẩm." + inline
  **`Button secondary sm` "Cập nhật số lượng còn 3"** + ghost "Xoá".
- `out-of-stock` — `color.danger` left border + "Sản phẩm đã hết hàng." + ghost "Xoá".
- `unavailable` — same treatment + "Sản phẩm hiện không còn được bán." + ghost "Xoá".
- `updating` — row at 60% opacity + inline spinner; controls disabled.

### 3.15 `OrderTotals` / `CartSummary`

Panel (`#FFFFFF` or `#F4F2EE`, `radius.md`, 16 padding):
```
Tạm tính (hàng hoá)            1.428.000 ₫      ← type.body, muted label
Phí vận chuyển                 Nhân viên sẽ xác nhận   ← plain text, NOT a warning style
────────────────────────────  (hairline)
Tổng tiền hàng                 1.428.000 ₫      ← type.price.total, #1F2A37
```
Never "Tổng thanh toán" / "Thành tiền". A `note` line under it: "Phí vận chuyển (nếu
có) sẽ được nhân viên thông báo khi xác nhận đơn." (`type.caption`, `#5B6673`).
`recalculated` boolean adds a small "Đã cập nhật" chip next to "Tổng tiền hàng".

### 3.16 `CartRevalidationNotice`

Banner at the top of the cart body when ≥1 line has an issue: `#FBF1E1` (warning tint)
or `#EAF2FB` (info tint if only price changes), `radius.sm`, 12 padding, 20px icon +
text: "Một vài sản phẩm cần được xem lại trước khi đặt hàng." No red wash. Links to the
first flagged line.

### 3.17 `CheckoutOrderSummary`

- **Desktop:** sticky right aside — item list (image 40×40, name 1-line clamp, `×qty`,
  line total), then `OrderTotals`.
- **Mobile:** collapsed bar at top — "3 sản phẩm • Tổng tiền hàng 1.428.000 ₫" +
  chevron; expands to the full list. The **full summary also repeats above the submit**.

### 3.18 `CheckoutNotice`

Inline block above the submit for `price-changed` / `stock-changed` / `unavailable`:
calm, `#EAF2FB` (price) or `#FBF1E1` (stock) tint, `radius.sm`, icon + short message +
what changed (old→new). Submit stays disabled/relabelled ("Xác nhận & đặt hàng") until
acknowledged. Never a full-screen or red-wash treatment.

### 3.19 `CheckoutSubmitButton`

`Button primary lg full-width`. States: `idle-enabled` ("Đặt hàng") · `idle-disabled`
(form invalid — still shows, press reveals errors + moves focus) · `revalidating`
("Đang kiểm tra giỏ hàng…" + spinner, disabled) · `submitting` ("Đang xử lý…" + spinner,
disabled, form dims to read-only) · `blocked` ("Xác nhận & đặt hàng" after a
`CheckoutNotice`) · `error-returned` (re-enabled; a calm notice above; **form + cart
preserved**).

### 3.20 `OrderStatusBadge`

Chip: 16px icon + text, `radius.xs`.
- `Đơn mới – chờ xác nhận` → info tone: `#EAF2FB` bg, `#1E4E8C` text, clock icon.
- `Đơn hàng đã được xác nhận` → success tone: `#E7F4EE` bg, `#177A4E` text, check icon.
- `Đã huỷ` → muted danger: `#FBECEC` bg, `#B02E2E` text, x-circle icon.
Never raw enum values.

### 3.21 `PaymentInstructions`

- **COD variant:** short calm card — 20px icon + "Bạn thanh toán bằng tiền mặt khi nhận
  hàng. Nhân viên sẽ xác nhận đơn và phí vận chuyển trước khi giao."
- **Bank-transfer variant:** a **prominent structured card** (`#FFFFFF`, `#E7E3DC`
  border, `radius.md`, 16 padding). Rows, each with a 32×32 copy button:
  `Ngân hàng` · `Chủ tài khoản` · `Số tài khoản` · **`Nội dung chuyển khoản` = the
  order code** (`ORD-001`) — visually emphasised. Values shown as
  **`[OWNER CONTENT TBD]`** until O8. Footer line: "Đơn hàng được xử lý sau khi cửa
  hàng xác nhận đã nhận chuyển khoản." **Never "Đã thanh toán".**

### 3.22 `OrderSummaryReadonly`

Shared by Success + Tracking-found: item rows (image 40, name, `×qty`, unit price, line
total) + `OrderTotals` (read-only) + shipping note. Safe fields only — no order UUID,
`source`, `payment_status` enum, COGS, batch data.

### 3.23 `TrackingForm`

One field, label + helper communicating **token OR pasted URL**:
label "Mã theo dõi đơn hàng", placeholder "Dán mã hoặc liên kết theo dõi bạn đã lưu",
helper "Ví dụ: `abc123…` hoặc `https://babywale…/dat-hang-thanh-cong/abc123…`".
Submit `Button primary` "Tra cứu". States: `idle` · `loading` (inline spinner) ·
`found` (renders `OrderSummaryReadonly` + `OrderStatusBadge` + `PaymentInstructions`) ·
`not-found` (one calm line: "Không tìm thấy đơn hàng với mã này. Vui lòng kiểm tra lại
đường dẫn hoặc liên hệ cửa hàng." — non-revealing) · `error` (retryable). **No
`order_number`-only path.**

### 3.24 `TrustItem` / `TrustBlock`

`TrustItem` = 20px line icon (`color.primary`, muted) + one true statement
(`type.bodySm`, `#5B6673`). `TrustBlock` = 2–4 `TrustItem`s in a row (desktop) / stack
(mobile), no panel fill, no badges. Content: "Thanh toán khi nhận hàng (COD) hoặc
chuyển khoản" · "Nhân viên xác nhận từng đơn trước khi xử lý" · "Hỗ trợ:
[SỐ ĐIỆN THOẠI / ZALO — OWNER CONTENT TBD]". Genuine-product line only if VD4 approves
exact wording. Optional reserved slot for a future "Chính sách" link (D4) — **renders
nothing until real content**.

### 3.25 `Footer`

`#FFFFFF`, top hairline. Groups (stack on mobile, 3–4 cols desktop):
**Baby Wale** (logo + "Cửa hàng mẹ và bé") · **Cửa hàng** (địa chỉ, điện thoại, giờ mở
cửa, Zalo/Facebook — all `[OWNER CONTENT TBD]`, a line omitted entirely if not
provided) · **Liên kết** (Sản phẩm, Danh mục, Giỏ hàng, Tra cứu đơn hàng) ·
**Chính sách** (slot — renders only if D4 content exists). Copyright line. **No account
link.**

### 3.26 `Toast`

Bottom-center (mobile) / bottom-right (desktop), `#1F2A37` bg, white text, `radius.sm`,
`elevation.overlay`, 16 padding, auto-dismiss ~4s, `aria-live="polite"`. Used for "Đã
thêm vào giỏ" (+ "Xem giỏ hàng" ghost link) and "Đã xoá khỏi giỏ".

### 3.27 `EmptyState`

Centered: optional 64px whale/wave line motif (`#E7E3DC`), `type.h2` heading, one
`type.body` `#5B6673` line, one `Button primary`. Variants: cart-empty ("Giỏ hàng của
bạn đang trống." → "Xem sản phẩm") · search-no-results · category-empty · catalog-empty.

### 3.28 `ErrorState` / `NotFoundState`

Centered, warm, minimal. `type.h2` heading, one calm line, recovery `Button`(s).
**No stack traces, no error codes, no dramatic sad illustration.** 404 → "Không tìm
thấy trang." + "Về trang chủ". Generic error → "Đã xảy ra sự cố. Vui lòng thử lại." +
"Thử lại".

### 3.29 `ImagePlaceholder` (missing product image)

`#F4F2EE` frame (matches the real image frame), a centered subtle whale/box line
motif (`#CDC8BE`, ~40% of frame), and the product name below in `type.caption`
`#5B6673`. **Never** a broken-image glyph, a stock photo, or a fake package render.

### 3.30 `Skeleton`

`#F1EFEB` blocks, `radius.sm`, a slow shimmer (`prefers-reduced-motion` → static). Card
skeleton = image square + 2 text bars + a short bar. PDP skeleton = gallery square +
title bars + price bar + button block.

---

## 4. Screen specifications

Each: mobile (390) section stack with heights/spacing, desktop (1440) notes, states,
components used. `[C: X]` = uses component X. Content is **mock** (labelled in §9).

### 4.1 Home — `/`

**Mobile (390), top→bottom:**

| Section | Height ≈ | Content |
| --- | --- | --- |
| `[C: MobileHeader]` | 104 (sticky) | logo, hamburger, cart, full-width search row |
| **Hero / value block** | ~230 | `#F4F2EE` surface, `radius.lg` bottom corners, faint wave motif top-right (very low contrast). H1 `type.display` "Đồ dùng chính hãng cho mẹ và bé" (mock). One `type.body` line. `[C: Button primary lg]` "Xem sản phẩm". **No second search box** (it's in the header). No gradient. |
| **Category shortcuts** | ~200 | H2 "Danh mục" + a 2-col grid of `[C: CategoryTile]` ×6 (icon-led). |
| **Sản phẩm mới** | ~360 | H2 "Sản phẩm mới" + horizontal scroll strip of `[C: ProductCard]` ×~6 (same card, ~150w in the strip). "Xem tất cả" ghost link. |
| **Trust block** | ~150 | `[C: TrustBlock]` — 3 `TrustItem`s stacked. |
| `[C: Footer]` | ~320 | groups stacked |

Above the fold at 390 (≈740 visible after header): hero (identity + CTA) + the top of
the category grid. Identity + search + a route to products — achieved.

**Desktop (1440):** header 64; hero as a centered band (content max 1200), H1
`type.display` desktop size, CTA inline; category tiles 6-up row; "Sản phẩm mới" a
4-up grid (no scroll); trust block a 3-up row; footer 4-col.

**States:** section-level degrade (a section that fails to load shows a small "Không
tải được mục này" with the rest of the page intact); "Sản phẩm mới" empty → section
hidden.

### 4.2 Catalog — `/san-pham`

**Mobile (390):**

| Section | Content |
| --- | --- |
| `[C: MobileHeader]` | sticky |
| Title row | H1 `type.h1` "Sản phẩm" · result count `type.bodySm` `#5B6673` "124 sản phẩm" |
| Control row (44h) | `[Danh mục ▾]` `[Sắp xếp ▾]` — two `Button secondary sm`; below them, active chips (`Bỉm & Tã ✕`) when set |
| Product grid | 2-col `[C: ProductCard]`, column-gap 12, row-gap 20 |
| Pagination | `‹ Trước · Trang 2 / 8 · Sau ›` — centered, `type.bodySm`, 44h targets |
| `[C: Footer]` | |

**Search variant (`?q=sua`):** H1 → "Kết quả cho "sữa"" + the query as a removable chip
("sữa ✕"). Grid unchanged (same `ProductCard`). **No-results:** `[C: EmptyState]`
search variant — "Không tìm thấy sản phẩm phù hợp với "sữa"." + "Xoá tìm kiếm" +
"Xem tất cả sản phẩm" + a row of category tiles.

**Category filter (`?danh-muc=…`):** identical to catalog with the category chip
active; canonical/H1 handling per S2.1 §5.2 (a pure category view routes to
`/danh-muc/[slug]`).

**Desktop (1440):** header 64; a control bar above the grid — H1 left, result count,
`Danh mục` dropdown + `Sắp xếp` dropdown + active chips right; grid 4-up (gap 24),
content max 1200; pagination numbered (`‹ 1 2 [3] 4 … 8 ›`).

**Loading:** `[C: Skeleton]` card grid. **Error:** `[C: ErrorState]` — "Không tải được
danh sách sản phẩm." + "Thử lại".

### 4.3 Category — `/danh-muc/[slug]`

Same as Catalog + a header block: `[C: Breadcrumbs]` (`Trang chủ / Bỉm & Tã`), H1 =
category name ("Bỉm & Tã"), one optional `type.body` description line. The category is
fixed (not a removable chip). Sort + pagination identical. **Not a separate visual
system** — the same `ProductGrid` / `ProductCard`.

### 4.4 Product Detail — `/san-pham/[slug]` (NORTH-STAR)

**Mobile (390), exact stack:**

| # | Section | Height ≈ | Detail |
| --- | --- | --- | --- |
| 1 | `[C: MobileHeader]` | 104 sticky | |
| 2 | `[C: Breadcrumbs]` mobile | 44 | `‹ Bỉm & Tã` |
| 3 | **Gallery** | 358 + 40 | square `#F4F2EE` frame, image `contain` + padding; below: a 5-dot page indicator + thumbnail strip (only if >1 image; here 4 thumbs 44×44). Tap image → enlarge dialog. `priority` (LCP). |
| 4 | **Purchase block** | ~300 | Name `[type.productName.pdp]` (H1) "Bỉm quần Merries size XL 38 miếng (12–20kg)" · Brand `type.caption` "MERRIES" · `[C: Price]` `type.price.lg` "489.000 ₫" · `[C: StockBadge]` "Còn hàng" · Unit `type.caption` "Gói 38 miếng" · `[C: QuantitySelector]` (label "Số lượng") · `[C: AddToCartButton]` full-width. Generous 16–20 gaps. Calm. |
| 5 | **(sticky) `[C: AddToCartBar]`** | 64 + safe area | appears after (4) scrolls past. `[mini Price "489.000 ₫"]` + `[Button primary "Thêm vào giỏ"]`. Top hairline + upward shadow. The **one** bottom transactional surface. |
| 6 | **Description** | auto | H2 "Mô tả" + `type.body` real copy (~4–6 lines mock). |
| 7 | **Specs** | auto | H2 "Thông tin sản phẩm" + key/value list (`type.bodySm`): Xuất xứ · Nhà sản xuất · Nhà phân phối. **Never** supplier/cost/batch. |
| 8 | `[C: TrustBlock]` | ~140 | 3 items. |
| 9 | **Related** | ~360 | H2 "Sản phẩm liên quan" + horizontal `[C: ProductCard]` strip (same card). |
| 10 | `[C: Footer]` | | |

**Out-of-stock variant (`ProductDetailOOS`):** (4) → `[C: StockBadge]` "Hết hàng";
`[C: AddToCartButton]` disabled, label "Hết hàng"; `[C: QuantitySelector]` disabled;
(5) sticky bar shows the disabled "Hết hàng" state; (9) Related moves **above** the
description (more presence / a path forward). Description + specs stay fully readable,
full contrast.

**Desktop (1440) — `ProductDetailDesktop`:** header 64 · breadcrumbs full · then a
2-col band (max 1200): **left** gallery ~56% (large square + vertical thumbnail rail),
**right** purchase panel ~40% (name H1 desktop size, brand, price `type.price.lg`
desktop, StockBadge, unit, QuantitySelector, AddToCartButton, a 3-item TrustBlock) —
**panel is sticky** (top offset = 64 + 24). Full-width below: Description · Specs ·
Related (4-up grid). **No sticky bottom bar on desktop** (the panel is always visible).

### 4.5 Cart drawer — (overlay, all pages)

Right-slide sheet, ~92% width (mobile) / 420w (desktop), `#FFFFFF`,
`elevation.overlay`, `radius.lg` left corners. Header: "Giỏ hàng (3)" + close 44×44.
Body: `[C: CartLineItem]` ×N (default state only — the drawer is a **quick projection**,
no full revalidation). A `type.caption` `#5B6673` note: "Giá và tồn kho được kiểm tra
khi thanh toán." Footer (pinned): `[Tổng tiền hàng 1.428.000 ₫]` · `[Button ghost
"Xem giỏ hàng"]` · `[Button primary lg "Tiến hành đặt hàng"]`. Focus-trapped; `Esc` /
scrim close → focus back to cart icon. **Empty:** `[C: EmptyState]` cart variant.

### 4.6 Cart page — `/gio-hang`

**Mobile (390):**

| Section | Content |
| --- | --- |
| `[C: MobileHeader]` | sticky |
| H1 | `type.h1` "Giỏ hàng" |
| `[C: CartRevalidationNotice]` | **only when issues** (this spec's canvas shows it, since it also shows a price-changed line) |
| `[C: CartLineItem]` ×N | one shown as `price-changed` (old 180.000 → new 185.000), rest `default` |
| `[C: OrderTotals]` | with the "Đã cập nhật" chip because a price changed |
| **(sticky) `[C: CartCheckoutBar]`** | `[Tổng tiền hàng]` + `[Button primary "Tiến hành đặt hàng"]` — **relabels to "Xác nhận giá mới & tiếp tục"** while an unacknowledged price change exists; **disabled** while any `qty-adjust` / `out-of-stock` / `unavailable` line exists (with a short reason line above it) |
| `[Button ghost "Tiếp tục mua sắm"]` | in-flow, above the footer |
| `[C: Footer]` | |

**Stock-changed / OOS / unavailable states:** shown at `CartLineItem` level (§3.14) —
`qty-adjust` line with "Cập nhật số lượng còn 3" + "Xoá"; `out-of-stock` /
`unavailable` lines with "Xoá"; the sticky bar disabled with "Vui lòng xem lại các sản
phẩm được đánh dấu".

**Empty:** `[C: EmptyState]` — "Giỏ hàng của bạn đang trống." + "Xem sản phẩm" +
category tiles. No sticky bar.

**Desktop (1440):** header 64 · H1 · 2-col (max 1200): **left** line items (wider
rows, image 80×80), **right** a panel with `OrderTotals` + `Button primary lg`
"Tiến hành đặt hàng" + ghost "Tiếp tục mua sắm". No sticky bar (panel visible).

### 4.7 Checkout — `/thanh-toan`

**Mobile (390) — minimal chrome, submit IN-FLOW (no bottom bar):**

| # | Section | Content |
| --- | --- | --- |
| 1 | Slim header (56h, sticky) | `[logo centered]` · `[‹ Quay lại giỏ hàng]` ghost link (left). **No** search, nav, or cart. |
| 2 | H1 | `type.h1` "Thanh toán" |
| 3 | `[C: CheckoutOrderSummary]` collapsed | "3 sản phẩm • Tổng tiền hàng 1.428.000 ₫" + chevron (expands to item list) |
| 4 | **Thông tin nhận hàng** | H2 · `[C: TextField]` Họ và tên* · `[C: TextField]` Số điện thoại* (`tel`) · `[C: TextArea]` Địa chỉ nhận hàng* · `[C: TextField]` Email (không bắt buộc) · `[C: TextArea]` Ghi chú (không bắt buộc). One per row. Visible labels. Realistic filled values in the canvas. |
| 5 | **Phương thức thanh toán** | H2 · `[C: PaymentMethodSelector]` — two `[C: PaymentRow]` (COD selected by default in the canvas). |
| 6 | **Xem lại đơn hàng** | H2 · full item list + `[C: OrderTotals]` + shipping note. |
| 7 | `[C: CheckoutNotice]` | rendered only in the price/stock-changed states. |
| 8 | `[C: CheckoutSubmitButton]` | full-width, in-flow, "Đặt hàng". |
| 9 | Reassurance | `type.bodySm` `#5B6673` "Nhân viên sẽ liên hệ xác nhận đơn hàng." + a 2-item mini `TrustBlock` (COD/transfer, human confirmation) directly under the submit. |
| 10 | Slim footer | copyright only. |

**States (component-level + at least one full-frame):**
- `validation-error` — `[C: TextField error]` on phone ("Số điện thoại không hợp lệ"),
  an error summary above the submit, focus moves to the first invalid field.
- `pending` — `[C: CheckoutSubmitButton submitting]` "Đang xử lý…"; form dims to
  read-only; **no full-screen spinner**.
- `server-error` — a calm `#FBECEC`-tint notice above the submit: "Không thể tạo đơn
  hàng. Vui lòng thử lại."; submit re-enabled; **all field values preserved**.
- `price-changed` / `stock-changed` — `[C: CheckoutNotice]` + submit → "Xác nhận & đặt
  hàng", blocked until acknowledged.

**Desktop (1440) — `CheckoutDesktop`:** slim header · 2-col (max 1120): **left** the
form sections (4–6 above, generous rhythm), **right** `[C: CheckoutOrderSummary]` as a
**sticky aside** (full item list + `OrderTotals` + shipping note + the reassurance line
+ mini TrustBlock). Submit at the end of the left column (in-flow). No marketing, no
cross-sell, no product nav.

### 4.8 Order Success — `/dat-hang-thanh-cong/[token]`

**Mobile (390) — COD variant (`SuccessCOD`):**

| # | Section | Content |
| --- | --- | --- |
| 1 | Slim header | logo centered only |
| 2 | Confirmation | a **modest** 40px check-in-circle (`color.success`), H1 `type.h1` "Đặt hàng thành công". **No** confetti / full-screen animation. |
| 3 | Order code | "Mã đơn hàng" label + `ORD-001` in `type.h2` `tabular`, selectable, + a 32×32 copy button. |
| 4 | Status | `[C: OrderStatusBadge]` "Đơn mới – chờ xác nhận" + one line "Nhân viên sẽ liên hệ với bạn để xác nhận đơn hàng." |
| 5 | `[C: PaymentInstructions]` COD | short calm card. |
| 6 | `[C: OrderSummaryReadonly]` | items + totals + shipping note. |
| 7 | Tracking utility | `[Button primary "Sao chép liên kết theo dõi"]` + `type.caption` "Lưu lại đường dẫn này để theo dõi đơn hàng." **No "Theo dõi đơn hàng" button to this same URL.** |
| 8 | Support | `[C: TrustItem]` "Cần hỗ trợ? Liên hệ [SỐ ĐIỆN THOẠI / ZALO — OWNER CONTENT TBD]". |
| 9 | `[Button ghost "Tiếp tục mua sắm"]` | secondary. |
| 10 | Slim footer | |

**Bank-transfer variant:** section 5 → `[C: PaymentInstructions]` bank-transfer (the
prominent structured card, values `[OWNER CONTENT TBD]`, memo = `ORD-001`, copy
buttons). **Never "Đã thanh toán".** Everything else identical.

**Desktop (1440):** a centered single column, max ~640 — same order; the bank-transfer
card slightly wider. Not a full-width layout.

### 4.9 Order Tracking — `/tra-cuu-don-hang`

**Idle:** slim header · H1 "Tra cứu đơn hàng" · `[C: TrackingForm]` (one field, label +
helper communicating **token OR pasted URL**, submit "Tra cứu") · a `TrustItem`
support line · footer.
**Found:** the same read-only view as Success (§4.8 sections 2–8, minus the
confirmation check — replaced by `[C: OrderStatusBadge]` at the top).
**Not-found:** the form with one calm message below it (non-revealing).
**Desktop:** centered single column, max ~560.

### 4.10 Supporting states

- **404 / generic error:** `[C: NotFoundState]` / `[C: ErrorState]` — slim header +
  centered content + recovery button + footer.
- **Empty cart:** `[C: EmptyState]` cart variant (also §4.6).
- **Missing product image:** `[C: ImagePlaceholder]` — shown in `ProductCard` and PDP
  gallery contexts (§3.29).

---

## 5. Responsive behavior

| Aspect | Mobile (≤767) | Tablet (768–1023) | Desktop (≥1024) |
| --- | --- | --- | --- |
| Header | 104h (56 + 48 search row), sticky | search row may inline | 64h, search inline center, sticky |
| Product grid | 2 col | 3 col | 4 col (max-width 1200) |
| PDP | single column; sticky bottom Add-to-Cart bar | single column, wider gallery | 2-col, sticky purchase panel, **no** bottom bar |
| Cart | single column; sticky bottom Checkout bar | single column | 2-col (items / summary panel), no bottom bar |
| Checkout | single column; collapsed summary at top + repeated above submit; **submit in-flow, no bottom bar** | single column | 2-col (form / sticky summary aside) |
| Filter/sort | bottom sheet | dropdowns | dropdowns |
| Nav | hamburger drawer | hamburger or inline | inline |
| Success/Tracking | single column, full-width | centered ≤640 | centered ≤640 |

Rules: no horizontal page scroll except deliberate carousels ("Sản phẩm mới", related,
gallery). Long names → 2-line clamp. Prices → `tabular`, never wrap mid-number. DOM
order = reading order at every breakpoint (2-col layouts are CSS placement only).

---

## 6. Accessibility decisions (built into the design)

- **Focus-visible** designed for: `Button` (all variants), `ProductCard` (ring around
  the whole card), `SearchField`, `TextField`/`TextArea`, `PaymentRow`,
  `QuantitySelector` (+/− and value), cart remove ✕, pagination links, breadcrumb links,
  hamburger, drawer close. Ring = `#1D6FE0` 2px + 2px `#FFFFFF` offset — visible on
  white, `#F4F2EE`, and on the Trust Blue button.
- **Status is never color-only:** `StockBadge` (icon + text), `OrderStatusBadge` (icon +
  text), `CartLineItem` change states (left-border **+** icon **+** text label),
  `PaymentRow` selected (border **+** radio **+** check **+** tint), field error (icon
  **+** border **+** message text). Price change: strikethrough **+** "Giá đã thay đổi"
  text, not just colour.
- **Labels visible** on every field (label above, never placeholder-as-label). Icon-only
  buttons carry `aria-label` in Vietnamese (cart, close, remove "Xoá {name} khỏi giỏ",
  qty +/−).
- **Form errors** associated (`aria-invalid` + `aria-describedby`); submit-time summary
  moves focus to the first invalid field.
- **Dialogs / drawers / sheets** (cart drawer, filter sheet, image enlarge, nav drawer):
  focus in → trap → `Esc` / scrim → focus returns to trigger.
- **Contrast:** all body/price/label/helper combos in §2.1 hit AA (most AAA). The two
  to re-verify at S2.4 with final values: the pink cart-badge numeral (`~4.0:1` — OK
  for ≤2 digits, fallback Trust Blue) and any white-on-`color.primary` small text
  (`5.9:1` — fine for `type.button`).
- **Touch targets** ≥ 44×44: qty +/−, remove, filter/sort buttons, pagination, nav,
  close, copy buttons.
- **Reduced motion:** `ProductCard` hover scale, gallery cross-fade, drawer/sheet
  slide, toast — all gated by `prefers-reduced-motion` (S1 already ships the global CSS
  block). No motion conveys state.
- **One `<h1>` per screen** (Home = the hero heading; PDP = the product name; Catalog =
  "Sản phẩm" / "Kết quả cho …"; etc.). No skipped heading levels.
- **No hover-only** discovery: gallery zoom, menus, tooltips all have tap/keyboard
  equivalents.

---

## 7. Long-content stress test (applied in the specs & canvas)

- Product names: "Sữa bột Aptamil Profutura Úc số 3 900g", "Tã quần Merries size XL 38
  miếng (12–20kg)", "Bình sữa Pigeon PPSU Plus cổ rộng 240ml" — the `ProductCard`
  reserves 2 lines (40px) so price/stock never shift; the PDP H1 wraps to 2–3 lines
  without pushing price below the fold.
- Category names: "Đồ dùng cho mẹ và bé" — tile label wraps to 2 lines, tile height
  fixed.
- Validation messages: "Số điện thoại không hợp lệ. Vui lòng nhập số di động 10 chữ số."
  — field grows, submit and summary reflow (Auto Layout).
- Large VND: "12.345.000 ₫" — `tabular`, fits the price slot and the totals row.
- 5+ cart items + a multi-line address + the bank-transfer instruction block — cart and
  checkout scroll cleanly; the sticky bar / summary stay correct.

---

## 8. Figma completeness matrix

"Canvas" = the Claude Design hi-fi canvas (§0, §12). "Spec" = fully specified in this
document (build-ready), not drawn as an artboard. "Ready?" = ready for S2.4 review and
for a designer/implementer to build.

| Screen | Mobile | Desktop | States covered | Reusable components | Ready? |
| --- | --- | --- | --- | --- | --- |
| Home | Canvas + Spec | Spec | section-degrade (spec) | Header, CategoryTile, ProductCard, TrustBlock, Footer, Button | ✅ |
| Catalog (default) | Canvas + Spec | Spec | loading, error, pagination (spec) | Header, ProductCard, Button, Pagination | ✅ |
| Catalog (search) | Spec | Spec | results, no-results | ProductCard, EmptyState | ✅ |
| Category | Spec | Spec | + breadcrumb, description | Breadcrumbs, ProductCard | ✅ |
| Search — no results | Spec | Spec | — | EmptyState, CategoryTile | ✅ |
| Product Detail (in stock) | **Canvas + Spec** (north-star) | **Canvas + Spec** | gallery, sticky bar | Header, Breadcrumbs, Price, StockBadge, QuantitySelector, AddToCartButton, TrustBlock, ProductCard, Footer | ✅ |
| Product Detail — OOS | **Canvas + Spec** | Spec | disabled CTA, related promoted | (as above) StockBadge OOS, AddToCartButton disabled | ✅ |
| Cart Drawer | Spec | Spec | default, empty, cached note | CartLineItem, OrderTotals, Button, EmptyState | ✅ |
| Cart Page | **Canvas + Spec** | Spec | price-changed (canvas), qty-adjust / OOS / unavailable (spec), empty (spec) | CartLineItem (all states), OrderTotals, CartRevalidationNotice, Button | ✅ |
| Cart — price changed | **Canvas** (in Cart) + Spec | Spec | shown | CartLineItem price-changed, Price strikethrough | ✅ |
| Cart — stock changed | Spec | Spec | qty-adjust, OOS, unavailable | CartLineItem states | ✅ |
| Checkout (idle/valid) | **Canvas + Spec** | **Canvas + Spec** | filled form, COD selected | TextField, TextArea, PaymentRow, CheckoutOrderSummary, OrderTotals, CheckoutSubmitButton, TrustBlock | ✅ |
| Checkout — validation error | Spec | Spec | field error + summary + focus | TextField error, CheckoutSubmitButton | ✅ |
| Checkout — pending | Spec | Spec | submitting, form read-only | CheckoutSubmitButton submitting | ✅ |
| Checkout — server error | Spec | Spec | calm notice, values preserved | CheckoutNotice / inline notice | ✅ |
| Checkout — price/stock changed | Spec | Spec | CheckoutNotice, blocked submit | CheckoutNotice | ✅ |
| Order Success — COD | **Canvas + Spec** | Spec | — | OrderStatusBadge, PaymentInstructions (COD), OrderSummaryReadonly, Button | ✅ |
| Order Success — bank transfer | Spec | Spec | prominent bank card, copy, memo | PaymentInstructions (bank) | ✅ |
| Order Tracking | Spec | Spec | idle, found, not-found, error | TrackingForm, OrderSummaryReadonly, OrderStatusBadge | ✅ |
| 404 / generic error | Spec | Spec | — | NotFoundState, ErrorState | ✅ |
| Empty cart | Canvas (state) + Spec | Spec | — | EmptyState | ✅ |
| Missing product image | Canvas (in components) + Spec | Spec | — | ImagePlaceholder | ✅ |
| **Foundations (tokens)** | **Canvas** | — | color / type / spacing / radius / elevation | — | ✅ (proposal) |
| **Components sheet** | **Canvas** | — | Button, Field, ProductCard states, Price, StockBadge, QuantitySelector, PaymentRow, OrderStatusBadge, TrustItem | — | ✅ |

**Every screen from the S2.3 brief is either drawn in the canvas or fully build-spec'd
here.** The canvas covers the highest-value / highest-risk screens (north-star mobile +
desktop, the reusability test, the trust test, the key change-state); the rest are
spec-complete for a Figma build with a Full/Dev seat or for direct implementation.

---

## 9. Content placeholders (mock / TBD register)

**Mock design content** (realistic, NOT real Baby Wale inventory — for density/layout
judgement only):

- Product names: Aptamil Profutura số 3 900g · NAN Optipro Plus 4 800g · Merries quần
  XL 38 · Moony Natural M 46 · Pigeon PPSU Plus 240ml · Mamamy khăn ướt 100 tờ · Meiji
  số 0–1 800g · Comotomo 250ml · Huggies Dry XL 44.
- Prices: 189.000 / 259.000 / 315.000 / 389.000 / 429.000 / 489.000 / 545.000 /
  625.000 / 749.000 ₫.
- Categories: Sữa cho bé · Bỉm & Tã · Bình sữa & Ti · Ăn dặm · Chăm sóc bé · Đồ dùng mẹ.
- Order code: `ORD-001`. Tracking token: `bw7f3k9q2m…` (illustrative).

**`[OWNER CONTENT TBD]`** — nothing rendered in production until supplied:

| Placeholder | Needed for | Gate |
| --- | --- | --- |
| Store address / phone / hours / Zalo / Facebook | Header ghost link, Footer, TrustBlock, Success support line | stakeholder |
| Bank account: name, number, bank, branch | `PaymentInstructions` bank-transfer | **O8** |
| Genuine-product claim exact wording | one optional `TrustItem` | **VD4** — omitted until approved |
| Return / exchange policy text | Footer "Chính sách" slot, PDP trust area | **D4** — slot renders nothing until real content |
| Home hero value line (final copy) | Home hero | S2.3 uses a mock; final copy S3 |
| Brand logo asset files (svg/png, sizes) | Header, Footer, OG | reuse supplied asset; **do not regenerate** — canvas uses a labelled "BABY WALE — LOGO PLACEHOLDER" wordmark |

---

## 10. Unresolved design questions for S2.4

Before freezing tokens, S2.4 must verify:

1. **Contrast with final values** — especially the pink cart-badge numeral (`~4.0:1` at
   `#D64C7C`) and any small white text on `color.primary`. Decide: keep pink badge or
   fall back to Trust Blue.
2. **Exact brand blue & pink** — sample from the supplied logo asset; adjust
   `color.primary` / `color.accent` and re-check all derived tints.
3. **Type scale** — ratify px/line-height per role; confirm diacritic clearance on
   `ProductCard` name and PDP H1 at real sizes; confirm `tabular-nums` on all price
   roles.
4. **Mobile gutters (16) & product-grid gaps (12/20)** — confirm 2-up card ≈171w is
   comfortable at 360–430px; check the smallest realistic device (360).
5. **ProductCard image aspect** — confirm **1:1** (vs 4:5) after seeing real
   supplier-source photos (VD3).
6. **Radii** — ratify the 6/10/14/20 scale; confirm buttons at `radius.sm` (10) read as
   "commerce", not enterprise-sharp or pill.
7. **Field height (44) / button heights (48/40/36) / sticky bar height (64) / mobile
   header (104)** — confirm the combined sticky-header + content + sticky-bottom-bar math
   leaves enough content viewport at 640–740px tall devices.
8. **Elevation values** — ratify the `raise` / `overlay` shadows; confirm the sticky
   bottom bar's upward shadow + top hairline reads as "detached" without heaviness.
9. **Container max-width (1200) & desktop grid (4-up)** — confirm density is
   "modern retail", not marketplace.
10. **Breakpoints** — ratify `480 / 768 / 1024 / 1280`; confirm PDP switches to 2-col at
    the right width and the checkout summary docks at the right width.
11. **Component-state completeness** — walk every component's variants (§3) against
    S2.1 §22 and confirm none is missing before it becomes a Tailwind/shadcn primitive.
12. **Dark mode** — confirm **light-only** for MVP; instruct S3 to remove or re-scope
    the S1 scaffold `@media (prefers-color-scheme: dark)` block when the real palette
    lands.
13. **Icon library** — pick one line-icon set (single stroke weight) at implementation;
    confirm 16/20/24 sizes and that every commerce icon is paired with text.
14. **Implementation feasibility** — confirm the token set maps cleanly to a Tailwind v4
    `@theme` + shadcn primitive layer (S3/S4) with no per-page overrides.

---

## 11. S2.4 handoff

**S2.4 receives:**

- This document — the exploratory token proposal (§2), full component anatomy + states
  (§3), per-screen build specs mobile + desktop (§4), responsive rules (§5),
  accessibility decisions (§6), the completeness matrix (§8), the placeholder register
  (§9), and the verification checklist (§10).
- The Claude Design hi-fi canvas (§12) — a viewable visual of the north-star and
  high-risk screens + a token board + a component state sheet.
- The S2.1 IA/flows contract and the S2.2 doctrine (unchanged).

**S2.4 must:**

1. Audit the token proposal; verify contrast with final brand values; **freeze** the
   token set (colours, type ramp, spacing, radius, elevation, container/grid,
   breakpoints, component sizes).
2. Ratify or adjust each component's variant/state set (§3) as the spec for the S3/S4
   primitive layer.
3. Resolve VD4 (genuine-product wording) and note D4 (returns policy) + O8 (bank
   details) as content gates — none block token freeze.
4. Decide light-only for MVP and instruct S3 on the scaffold dark-mode block.
5. Produce the **DESIGN APPROVED** artifact (frozen tokens + component contract) that
   S3 (`design-system` skill) turns into the Tailwind `@theme` + shadcn primitives, and
   that S4+ implements screens against with `figma-to-code` discipline.
6. **Optional:** if a Figma Full/Dev seat becomes available, build the Figma file from
   §3–§4 and this document — the structure (pages `00 Cover` … `10 Handoff`), components,
   and screen frames are all specified.

**S2.3 does NOT mean DESIGN APPROVED.** The user reviews the hi-fi canvas + this spec;
S2.4 refines and freezes.

---

## 12. Claude Design canvas

**Link:** https://claude.ai/code/artifact/378cf72c-a832-4c2d-8e36-c6ac30c74640
(title "Baby Wale Storefront — Hi-Fi", version 2). A private Claude Design canvas —
pan/zoom, click-to-select, PNG/PDF export; opens on the Mobile page.

**Structure (9 artboards, 3 pages):**

| Page | Artboards |
| --- | --- |
| **Foundations & Components** | `Foundations` — colour roles (with contrast notes) · Be Vietnam Pro type ramp · spacing / radius / elevation · the image system. `Components` — Button (primary/secondary/ghost + hover/focus/disabled/pending) · TextField (default/focus/filled/error/disabled) · Price · StockBadge · OrderStatusBadge · QuantitySelector · PaymentRow (selected = border + radio + tint) · `ProductCard` (default / focus / long-name / out-of-stock / missing-image) · CartLineItem (default / price-changed / quantity-adjustment) · TrustItem. |
| **Mobile (390)** | `Main` = **Product Detail (north-star)** — breadcrumb, square gallery + thumbs, purchase block, **sticky bottom Add-to-Cart bar**, specs, trust, related. `Home` — static hero + `Xem sản phẩm` + icon-led category tiles + "Sản phẩm mới" strip + trust + footer. `Catalog` — "Kết quả cho …" + Danh mục/Sắp xếp controls + query chip + 2-col grid (one card OOS) + pagination. `Cart` — revalidation notice + a **price-changed line** (old→new) + totals with "Đã cập nhật" + **sticky Checkout bar** ("Xác nhận giá mới & tiếp tục"). `Checkout` — slim header, collapsed summary, filled contact form, payment rows (COD selected), review, **submit in-flow (no bottom bar)**, quiet trust cues. `SuccessCOD` — modest check + "Đặt hàng thành công", copyable `ORD-001`, status chip, COD instructions, read-only summary, **"Sao chép liên kết theo dõi"**, support. |
| **Desktop (1440)** | `ProductDetailDesktop` — sticky header, breadcrumb, gallery left + **sticky purchase panel** right, description/specs 2-col, related 4-up, footer. Same tokens/components as mobile. |

**Not drawn as artboards (fully build-spec'd in §3–§4 instead):** Category landing,
Search no-results, Cart Drawer, Cart stock-changed/unavailable, Checkout
error/pending/blocked full-frames, Order Success bank-transfer, Order Tracking, 404 /
empty cart / missing-image full screens, desktop Catalog/Cart/Checkout. The
`ProductCard` OOS / missing-image and `CartLineItem` change states **are** on the
Components artboard.

**Every value in the canvas is the exploratory proposal from §2 — NOT approved tokens.**
Product images are shown as the neutral 1:1 `contain` frame (no real photos exist; none
fabricated) — real supplier photography drops into the identical frame.

**Visual QA:** the canvas was authored blind — **no browser/screenshot tool was
available to inspect the rendered artboards in this session.** The `.dc.html` sources
were self-reviewed against the format rules and the doctrine (one gradient fix + one
dead CSS rule removed before republish). Frame heights were sized from summed vertical
rhythm + ~8% slack; a footer could clip on the tallest artboards if Be Vietnam Pro
renders taller than estimated — reflow/re-seed if so. S2.4 should open the canvas and
confirm no clipping / overflow / misalignment before relying on it for pixel decisions.
